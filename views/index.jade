extends layout

block content
  if (!user)
    //a(href="/login") Login
    //br
    //a(href="/register") Register
  if (user)
    p.text-right #{user.name}
    //a(href="/logout") Logout
    //br
    a(href="/memo") Memo
